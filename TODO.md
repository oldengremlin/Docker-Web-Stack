# TODO

Задумане, але ще не зроблене. Кожен пункт — з причиною, чому він тут, і
з тим, що вже з'ясовано, щоб не починати з нуля.

## `dcpmgmt migration` — перенесення домену між серверами

Не плутати з `docs/wordpress-migration.md`: там сайт переїжджає **на інший
домен у межах одного сервера**. Тут — **той самий домен на інший сервер**.

Задум:

```bash
# на джерелі
dcpmgmt migration export example.com --out example.tar

# на цільовому
dcpmgmt migration import --in example.tar [example.com] [--dry-run]
```

`export` нічого не гасить: між підготовкою і власне переїздом може минути
тиждень. Гасіння джерела — окремий свідомий крок.

### Починати з маніфесту

Від того, що в ньому лежить, залежить решта. Щонайменше: домен і тип,
версія `dcpmgmt`, дата, чим зроблено дамп (`MySQL 8.0.26` / `MariaDB 13`),
перелік доменів у комплекті, контрольні суми, походження пароля.

### Що їде в архіві

- дамп БД — **тільки `mysqldump`**, ніколи datadir (див. нижче);
- `web/` — з `--numeric-owner`, інакше на цільовому поїдуть права;
- `nginx/custom/<домен>.conf` і `.php.conf`;
- `<sites_root>/<домен>/php/*.ini`;
- запис реєстру;
- сертифікат із `live/`, `archive/`, `renewal/` — див. «Сертифікати».

Не їде: datadir, `wp-content/cache` і решта кешів.

### Підводні камені

**Майстер-ключ і `password_mode = derived`.** Найпідступніше. Derived-пароль
— це HMAC(ключ сервера, домен), а `~/.dcp-master-key` на кожному сервері
свій. На цільовому виведеться інший пароль, тоді як у дампі вже лежать
гранти зі старим, а `wp-config.php` у `web/` містить старий відкритим
текстом. Отже: або архів фіксує фактичний пароль і домен стає `random`,
або цільовий сервер має той самий ключ.

Ховати пароль в архіві безглуздо: `wp-config.php` будується як
`getenv_docker('WORDPRESS_DB_PASSWORD', '<пароль>')`, тобто містить його
відкритим текстом другим аргументом — а `web/` їде цілком. Тож вибір не
між «видно пароль» і «не видно», а лише в тому, чи лишається він дійсним
після міграції. Найпростіше: передати як є, а на цільовому перевипустити
під тамтешній ключ (див. «Зміна режиму пароля») — тоді пізній витік
архіву коштує менше.

**Архів — секрет цілком.** Навіть без пароля БД він везе дамп із хешами
паролів усіх користувачів WordPress, вмістом форм і даними клієнтів.
0600, не в git, видалити після міграції — і сказати про це так само
голосно, як сказано про `secrets/`.

**`wp-config.php` — джерело істини для БД, не реєстр.** Імена БД,
користувача і `DB_HOST` зашиті у файл, який їде разом із `web/`. `import`
мусить вивести `db_name`/`db_user` з нього або звірити й вилаятись при
розбіжності — інакше буде свіжа БД з правильною назвою і WordPress, який
стукає в іншу.

**Дамп, а не datadir.** Перевірено на живому переїзді: datadir від MySQL 8
MariaDB не відкриває взагалі — `Unsupported redo log format`. Маніфест має
фіксувати джерело дампа, щоб `import` сам вирішив, чи потрібна заміна
`utf8mb4_0900_ai_ci`, якої MariaDB не знає.

**Сертифікати — вилка.** Не везти: після перемикання DNS `cert issue`
спрацює, але між перемиканням і видачею сайт віддає помилку TLS. Везти:
перемикання безшовне, але `renewal/<домен>.conf` посилається на
`account_id` з `/etc/letsencrypt/accounts/`, а акаунт на цільовому інший —
подовження тихо зламається. Схиляємося: везти **і** одразу після
перемикання DNS робити `cert issue --force`, щоб renewal переписався під
локальний акаунт. Заразом навчити `doctor` помічати renewal, який
посилається на неіснуючий акаунт.

**DNS — не лише сам запис.** TTL знижується за добу-дві **до** `export`.
Між `export` і перемиканням сайт живий і приймає записи — коментарі,
замовлення, завантаження, — і в архів вони не потрапили. Після
перемикання частина трафіку ще йде на старий сервер через кешований DNS,
дві копії розходяться, і зливати їх потім нічим. Отже `export` має бути
дешевим для повторного прогону, а сценарій — передбачати коротку заморозку
джерела перед фінальним дампом. Можливо, окрема `migration freeze`
(503 або редірект на джерелі).

**Склад комплекту.** Разом із доменом їдуть усі `type = redirect` з
`target = <домен>` — у реєстрі це видно. Але в кожного свій DNS і свій
сертифікат, тож `export` має друкувати повний чекліст того, що треба
перемкнути: забути один редірект означає лишити домен висіти в нікуди.
`proxy`-домени так просто мігрувати не можна — вони посилаються на
`upstream`, якого на цільовому немає; або відмова, або явне підтвердження.

**Перевірка до перемикання DNS.** Найцінніше, що може дати `import`:
підняти і перевірити сайт, поки на нього ще ніхто не йде —
`curl --resolve example.com:443:<new-ip> https://example.com/`. Варто
друкувати готову команду в підказці після імпорту.

**Дрібне, але кусається.** `uploads` на живому сайті — гігабайти, тож
варто дозволити каталог замість одного `.tar` (великі дані `rsync`,
метадані архівом). `quic_reuseport_domain` на цільовому може бути
незаповненим — `doctor` це вже ловить. Конфлікт `sid` з наявним доменом —
відмова, як в `add`.

### Зауваги до CLI

- `--to` вже означає ціль редіректу (`add redirect ... --to ...`). Для
  файла — `--out`/`--in` або позиційний аргумент.
- Ім'я домену в `import` надлишкове (воно в маніфесті), але корисне як
  запобіжник: вказане й не збіглося — відмова.
- `--dry-run` на `import` обов'язковий: що з'явиться в реєстрі, які
  контейнери створяться, звідки візьметься пароль — і нічого не робити.

## Зміна режиму пароля на живому домені

```bash
dcpmgmt secrets rotate example.com [--mode derived|random]
```

Лягає в наявну родину `secrets show|restore|key`.

Навіщо, у порядку ваги:

1. **Ротація скомпрометованого пароля.** Зараз відповіді на «пароль
   засвітився» в інструмента немає взагалі.
2. **Успадковані домени.** Вони всі `random`, з паролями невідомого
   походження. Переведення на `derived` робить `secrets restore`
   самодостатнім: каталог відновлюється з самого лише майстер-ключа, і
   бекапити `secrets/` окремо стає непотрібно — саме те, що вже обіцяно
   в README про цей режим.
3. **Міграція.** На цільовому сервері — один виклик замість ручної
   возні з `ALTER USER`.

**Root не потрібен.** Здавалося б, зміна пароля вимагає root, а в нас
`MYSQL_RANDOM_ROOT_PASSWORD` — тобто root-пароль випадковий і нікому не
відомий. Але свій власний пароль користувач змінює сам:

```sql
ALTER USER USER() IDENTIFIED BY '...';
```

Підключаємося старим паролем від імені користувача домену — і цього
досить.

Порядок операцій — саме такий, бо він робить відкат безкоштовним:

1. обчислити новий пароль за режимом;
2. переписати `secrets/<домен>.env` (0600), лишивши `.bak` — як це вже
   робить `write_file` для всього іншого;
3. `ALTER USER USER()` у контейнері БД;
4. **якщо крок 3 провалився** — відновити `secrets/` з `.bak` і сказати
   про це; жоден зі станів не лишає домен без доступу до власної бази:
   або обидва старі, або обидва нові;
5. оновити `password_mode` у реєстрі;
6. перестворити контейнер WordPress, щоб `env_file` підхопився.

Чого робити НЕ треба:

- **правити `wp-config.php`.** Пароль там лежить дефолтом у
  `getenv_docker`, але оточення має пріоритет, тож робочому сайту
  застарілий дефолт байдужий. При відновленні з порожнього `web/`
  entrypoint створює файл заново з поточних env — старий дефолт просто
  зникає. Значення він мав би лише для запуску WordPress геть без
  оточення, а це не наш випадок.

Вікно недоступності в кілька секунд між кроками 3 і 6 — властивість
задачі, а не недогляд: звести його до нуля можна було б лише через
dual password (`ALTER USER … RETAIN CURRENT PASSWORD`), а це фіча
MySQL 8, якої в MariaDB немає. Отже команда просто попереджає про нього
перед виконанням, як `apply` попереджає про перестворення контейнерів.

## Нові типи доменів

Наш `type = wordpress` насправді вже означає «php-fpm-застосунок із БД»;
WordPress там — лише набір значень за замовчуванням. Звідси черга
кандидатів, у порядку пріоритету.

Спільна засторога для всіх: `latest` у цих образів — **апачевий**
(перевірено: у `joomla`, `drupal` і `mediawiki` `latest` має той самий
digest, що й `apache`). Наша перевірка `image_not_fpm` відхилить їх
правильно, навіть нічого не знаючи про самі движки.

### MediaWiki — найвищий пріоритет

Дві окремі задачі: навчити стек піднімати сучасний MediaWiki і перевезти
успадковану вікі 1.35 у нього.

**Маршрут оновлення виявився коротшим, ніж здається.** З 1.36 MediaWiki
підтримує оновлення **лише з двох останніх LTS**. LTS — кожен четвертий
реліз, приблизно раз на два роки: 1.35, 1.39, 1.43. Отже:

- `1.35 → 1.43` — **підтримується напряму**, одна сходинка;
- `1.35 → 1.46` — ні: для 1.46 двома останніми LTS є 1.39 і 1.43.

**Цільова версія — 1.43 LTS, а не 1.46.** LTS-гілка отримує виправлення
три роки від релізу (1.43.0 — грудень 2024), звичайна — близько року.
Для вікі, яку не хочеться чіпати щопівроку, це різниця між «раз на три
роки» і «двічі на рік». Образ `mediawiki:1.43-fpm` є.

Варіант `-fips` брати не треба: це криптомодуль під вимоги
держсектору США, зайва складність без жодної користі тут.

**Візуальний редактор полагодиться сам.** Parsoid переписано на PHP і
вбудовано в ядро ще з 1.35, тож VisualEditor працює без окремого
Node-сервісу і без RESTBase. Класична поломка «оновилися — втратили
візуальний редактор» родом саме з часів, коли Parsoid був зовнішнім
сервісом.

**Головне непорозуміння, якого варто уникнути.** «Підняти новий стек і
перенести дані» і «оновити наявну інсталяцію» — з погляду MediaWiki це
та сама операція, якщо переносити **базу даних**. Дані вікі — це БД плюс
`images/`; щойно дамп зі схемою 1.35 потрапляє в контейнер 1.46, перше,
що доведеться зробити, — `maintenance/update.php`, і саме він упреться в
обмеження «дві останні LTS». Новий контейнер тут нічого не рятує:
обмеження стосується схеми БД, а не каталогу з кодом.

Обійти це можна єдиним способом — **не переносити схему взагалі**.

**Дві стратегії перевезення:**

| | що робить | що зберігає | що губить |
|---|---|---|---|
| `update.php` | оновлює схему БД на місці | геть усе: історію, користувачів, логи, watchlist, права | — |
| XML-дамп | `dumpBackup.php --full` → `importDump.php` у чисту інсталяцію | статті з повною історією версій | користувачів, права, логи дій, налаштування |

Звідси два різні маршрути, і обирати треба свідомо:

- **Мета — зберегти все** (користувачі, права, логи, watchlist): переносимо
  БД і йдемо сходинками. `1.35 → 1.43` підтримується напряму, тож сходинка
  лише одна; далі за бажанням `1.43 → 1.46`. У Docker це дешево: та сама
  БД, послідовно два контейнери з `update.php`.
- **Мета — чистий новий стек**, а вміст перенести: `dumpBackup.php --full`
  на старій, чиста інсталяція потрібної версії, `importDump.php`. Схема БД
  не переїжджає зовсім, тому обмеження LTS не діє взагалі — можна одразу
  в 1.46. Ціна: користувачі, права й логи не переїдуть, статті з повною
  історією версій — переїдуть.

Для вікі з робочими інструкціями другий маршрут часто кращий: чистий
старт без двадцяти років накопиченого мотлоху в налаштуваннях, а історія
статей збережена.

**Спершу з'ясувати точну версію джерела.** На `Спеціальна:Версія` було
`1.35.4` — і це важливо: 1.35 є LTS, а 1.36 (сусідня цифра, яку легко
сплутати) — ні. Від цього залежить, чи можлива сходинка в 1.43 напряму.

**Чого не робити:** копіювати старий `LocalSettings.php` дослівно. Краще
згенерувати новий веб-інсталятором і перенести з нього лише свої
налаштування. Каталог `images/` копіюється як є, далі `refreshLinks.php`
і `rebuildImages.php`.

**Ризик близький до нуля**, бо все робиться на копії: дамп БД плюс
`images/` піднімаються в окремому контейнері, жива вікі не чіпається до
моменту перемикання. Той самий сценарій, що ми вже двічі відпрацювали.

### Joomla — дешево, коли дійдуть руки

Образ влаштований як у WordPress: entrypoint розгортає ядро в порожній
`/var/www/html`, движок оновлює себе сам через адмінку, конфігурація
через `JOOMLA_DB_*`. Від `domain-wordpress.yml.tmpl` відрізняється
назвами змінних і дрібницями в `try_files`. Приблизно один вечір.

### Nextcloud і Matomo — не CMS, але свої в дошку

Обидва мають офіційні php-fpm образи і лягають у стек тим самим типом.
Matomo цікавий практично: власна аналітика замість зовнішньої, на своєму
сервері.

### Ghost — найнижчий пріоритет, і тип для нього вже є

Ghost — Node.js, а не PHP. У стек лягає не як `wordpress`, а як `proxy`:
контейнер слухає HTTP, nginx просто проксіює. Тобто окремий тип не
потрібен зовсім — досить `add proxy`.

## Синхронність інструмента й інсталяції

`dcpmgmt`, `templates/` і `tools/` копіюються в інсталяцію вручну, трьома
різними командами. `dcpmgmt version` показує версію самого скрипта, тож
старі шаблони поруч із новим кодом виглядають як справний стек. Одного
разу це вже мало не спричинило біду: версія оновилася, шаблони — ні.

Задум: зашити в код хеш `templates/` і лаятись, коли вони розійшлися.
Покриє і `tools/`.
